[platformio]
; Repo root convenience wrapper.
; Allows opening the repository root in VSCode/PlatformIO while the actual
; firmware lives in `firmware/`.
extra_configs = firmware/platformio.ini

src_dir = firmware/src
include_dir = firmware/include
lib_dir = firmware/lib
test_dir = firmware/test

